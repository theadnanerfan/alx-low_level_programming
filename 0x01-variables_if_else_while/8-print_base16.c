#include <stdio.h>

/**
 * main - print all numbers less than 16
 * with putchar then new line
 * Return: 0
 */

int main(void)
{
int n = 0;
int a = 'a';

while (n < 10)
{
putchar(a);
n += 1;
}
while (a < 'f')
{
putchar(a);
a += 1;
return (0);
}
}
